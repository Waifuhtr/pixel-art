venv/
__pycache__/

# Packaged deliverables built from tracked sources (e.g. the Space zip) —
# committing one would duplicate files git already has and go stale on the
# next edit.
*.zip
*.db
output/
references/
models/
.env
service-account.json

# Local IDE / agent config
.claude/
